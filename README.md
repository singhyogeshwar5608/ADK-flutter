# ADK-flutter

NetShop / ADK partner portal — Flutter app (`netshop_flutter`).

## Run

```bash
flutter pub get
flutter run
```

Copy `.env.example` to `.env` and set `API_BASE_URL` (and other keys as needed).

## Docs

- [Flutter documentation](https://docs.flutter.dev/)

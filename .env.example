# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=dtnuwjtt3
CLOUDINARY_UPLOAD_PRESET=ADK_preset
CLOUDINARY_UPLOAD_URL=https://api.cloudinary.com/v1_1/dtnuwjtt3/image/upload

# API Configuration
API_BASE_URL=http://localhost:8000/api/v1

# Flutter: copy this file to both `.env` (optional) and `assets/dotenv` so web/mobile can load env from assets.
# Do not put server-only secrets in `assets/dotenv` — it is bundled into the client.

# Full URL of the signup page (no trailing ?). Shown/copied when API referral link is unavailable; should match backend APP_SIGNUP_URL.
REFERRAL_SIGNUP_BASE_URL=https://aslidesikisan.netlify.app/signup

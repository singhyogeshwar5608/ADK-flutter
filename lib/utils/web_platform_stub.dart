import 'package:flutter/widgets.dart';

/// Stub for web platform view registration to avoid compilation errors on mobile.
void registerWebView(String viewId, String url) {
  // Do nothing on mobile
}
